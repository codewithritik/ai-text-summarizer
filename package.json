{
  "name": "ai-text-summarizer",
  "version": "1.0.0",
  "description": "Chrome extension for AI text summarization",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "@google/genai": "^1.29.0",
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
